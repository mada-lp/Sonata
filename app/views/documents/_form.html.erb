<%= simple_form_for(document) do |f| %>
  <%= f.input :title, label: "Insira um título" %>
  <%= f.input :content, label: "Digite o conteúdo" %>
  <%= f.input :file %>
  <%= f.submit 'Salvar', class: "btn btn-primary" %>
<% end %>
